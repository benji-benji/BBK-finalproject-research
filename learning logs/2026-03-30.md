##### Walk in Python 

iterating through a sequence

`get_module` walks `model.named_modules()` means:

```python
for n, m in model.named_modules():
    if n == name:
        return m
```

It goes through every module in the model one by one, checking if the name matches.

##### **tuple unpacking** (also called destructuring).

The comma on the left side of `=` (or `in`) means "split this iterable into these variables positionally."

The rule: if the right side produces an iterable of N elements, the left side can have N comma-separated names, and Python assigns them by position.

##### 'With' Statement & making custom Context Managers
Where plain 'with' statement requires action to be coded in the with statment, 
you can also make a context manager class - which contains the actions 
and just call the class 


contextlib.AbstractContextManager 

contextlib https://docs.python.org/3/library/contextlib.html#examples-and-recipes

Abstract from PEP (Python Enhancement Proposal):
https://peps.python.org/pep-0343/
	This PEP adds a new statement “with” to the Python language to make it possible to factor out standard uses of `try/finally` statements.
	
	In this PEP, context managers provide `__enter__()` and `__exit__()` methods that are invoked on entry to and exit from the body of the with statement.

Context managers are a Built-in Type ( https://docs.python.org/3/library/stdtypes.html#typecontextmanager ):
contextmanager.__enter__()
contextmanager.__exit__()

https://docs.python.org/3/reference/compound_stmts.html#with
https://docs.python.org/3/reference/datamodel.html#context-managers


##### Python Closures 

Closures in Python are like “memory-equipped” functions. They allow a function to remember values from the environment in which it was created even if that environment no longer exists.

A closure is formed when:

- A function is defined inside another function (nested function).
- The ****inner function**** references variables from the ****outer function****.
- The ****outer function**** returns the ****inner function****.




##### Token Position in Transformers  

At any single layer, the output tensor has shape:

```
[batch_size, sequence_length, hidden_dim]
[    11,           5,           1600    ]
```

`Trace` hooks one of those layers and gives you that one tensor. The loop in `trace_important_states` runs `trace_with_patch` 48 times, each time hooking a different layer and getting that layer's tensor.

